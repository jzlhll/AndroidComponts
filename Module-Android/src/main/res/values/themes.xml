<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="StyleBottomSheetStyle" parent="Widget.Material3.BottomSheet.Modal">
        <item name="shapeAppearance">@null</item>
    </style>

    <style name="StyleBottomSheetDialogTheme" parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/StyleBottomSheetStyle</item>
        <item name="enableEdgeToEdge">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- ShapeableImageView shapeAppearanceOverlay 圆角-->
    <style name="shape_round8dp_Style">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
        <!-- app:strokeColor
app:strokeWidth -->
    </style>

    <!-- ShapeableImageView shapeAppearanceOverlay 圆形 -->
    <style name="shape_circle_Style">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>